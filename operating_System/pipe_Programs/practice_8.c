#include <stdio.h>
#include <string.h>

int main(int argc, char const *argv[])
{
    printf("Trying run-time, direct string input");
    char arr[10];
    // scanf("%s", arr);
    // printf("\n");
    // for (int i = 0; i < strlen(arr); i++)
    // {
    //     printf("%c ", arr[i]);
    // }
    printf("leng is : %d\n", strlen(arr));
    printf("\n");
    
    return 0;
}